#include <stdio.h>
#include <math.h>
void main()
{

    int n;
    scanf("%d",&n);

    if ((int)sqrt(n) - sqrt(n)==0) printf("%d la so chinh phuong",n);
    else printf("%d khong la so chinh phuong",n);

}
